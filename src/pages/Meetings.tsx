import { StubPage } from "@/components/shared/StubPage";
import { Sparkles } from "lucide-react";
export default function Meetings() {
  return <StubPage title="Reuniões IA" description="Upload de gravações, transcrição automática, resumo e pontos-chave." icon={Sparkles} />;
}
