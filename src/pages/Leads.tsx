import { StubPage } from "@/components/shared/StubPage";
import { Users } from "lucide-react";
export default function Leads() {
  return <StubPage title="Leads" description="Gerencie todos os seus leads. Use o Kanban para uma visão mais visual." icon={Users} />;
}
